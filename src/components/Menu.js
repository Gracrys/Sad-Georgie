import React from 'react';
import ReactDOM from 'react-dom';

const Menu = () => {
	return (
		<nav className="menu">
			<button> Tag</button>
			<button> Tag</button>
			<button> Tag</button>
			<button> Hard </button>
		</nav>
	);
};
export default Menu;
